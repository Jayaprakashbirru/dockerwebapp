version: "3.9"

services:
  dockerapp:
    build: ./Docker-db
    networks:
      - mynetwork

  application:
    build: ./Docker-app
    ports:
      - "8080:8080"
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: bridge 
